At the first time:
make config

To test if the library works:
make compile

To compile the client:
make compile-client

To compile the server:
make compile-server

To compile and launch the client:
make client <arguments>

To compile and launch the server:
make server <arguments>

To produce doxygen documentation:
make doxygen

To produce cscope files:
make cscope